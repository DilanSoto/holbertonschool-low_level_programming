#include <stdio.h>
#include <string.h>

/**
 * puts2 - print character
 * *str - pointers
 * Return: void
*/

void puts2(char *str)
{
	char *str;

	int m;

	for (str[m] = 0, m != '\0',)
	{
		putchar(str[m]);
		m += 2;
	}
	{
		putchar ('\n'):
	}
			return (void)
}
