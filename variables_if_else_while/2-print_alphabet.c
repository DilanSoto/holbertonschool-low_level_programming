#include <stdio.h> 
/**
*main - Entry Point
*
*Return: Always 0 (success)
*/
int main(void)
{
	char letras;

	for (letras = 'a'; letras <= 'z'; letras++)
		{
		putchar (letras);
		}
		putchar ('\n');
return (0);
}
